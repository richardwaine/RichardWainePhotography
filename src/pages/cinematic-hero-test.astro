---
// @ts-nocheck
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scroll Zoom Effect</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        /* ===== BASE STYLES ===== */
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'Inter', sans-serif;
            /* background: #0d0d0d; */
            background:var(--drkbg);
            color: white;
            overflow-x: hidden;
        }

        /* ===== NAVIGATION ===== */
        .nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 9999;
            padding: 25px 60px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 80%;
            margin: 0 auto;
        }

        .nav__logo img {
            height: 120px;
        }

        .nav__cta {
            background: var(--accent);
            border: none;
            padding: 14px 28px;
            color: white;
            font-weight: 700;
            font-size: 0.85rem;
            letter-spacing: 1px;
            text-transform: uppercase;
            cursor: pointer;
            clip-path: polygon(8px 0, 100% 0, calc(100% - 8px) 100%, 0 100%);
            transition: all 0.3s ease;
        }

        .nav__cta:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(3, 169, 244, 0.3);
        }

        /* ===== HERO SECTION ===== */
        .hero {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            transform-origin: center center;
            z-index: 1;
            animation: zoom-animation linear;
            animation-timeline: scroll(root);
            animation-range: 0vh 300vh;
        }

        @keyframes zoom-animation {
            0% { 
                transform: scale(1) translateZ(0);
                opacity: 1;
            }
            70% { 
                transform: scale(3) translateZ(0);
                opacity: 0.3;
            }
            100% { 
                transform: scale(6) translateX(-100vw);
                opacity: 0;
            }
        }

        .hero__content {
            display: flex;
            gap: 80px;
            align-items: center;
            max-width: 1400px;
            background: rgba(10, 10, 10, 0.2);
            backdrop-filter: blur(20px);
            border-radius: 30px;
            border: 1px solid rgba(3, 169, 244, 0.1);
            /* padding: 60px; */
            padding:120px;
            box-shadow: 
                0 0 60px rgba(3, 169, 244, 0.1),
                inset 0 1px 1px rgba(255, 255, 255, 0.05);

        }

        .hero__text {
            flex: 1;
        }

        .hero__title {
            font-size: 4.5rem;
            font-weight: 900;
            line-height: 0.85;
            margin-bottom: 30px;
            letter-spacing: -3px;
            text-transform: uppercase;
        }

        .hero__title--highlight {
            background: linear-gradient(135deg, var(--accent) 0%, rgba(0, 168, 255, 0.7) 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero__subtitle {
            font-size: 1.3rem;
            color: #cccccc;
            line-height: 1.6;
            margin-bottom: 50px;
            max-width: 520px;
        }

        .hero__actions {
            display: flex;
            gap: 30px;
            align-items: center;
        }

        .hero__image {
            flex: 1;
        }

        .hero__image img {
            width: 800px;
            height: 500px;
            object-fit: cover;
            object-position: top center;
            border-radius: 20px;
            box-shadow: 30px 30px 80px rgba(0, 0, 0, 0.5);
        }

        /* ===== BUTTONS ===== */
        .btn {
            border: none;
            font-weight: 700;
            font-size: 0.95rem;
            letter-spacing: 1px;
            text-transform: uppercase;
            cursor: pointer;
            clip-path: polygon(8px 0, 100% 0, calc(100% - 8px) 100%, 0 100%);
            transition: all 0.3s ease;
        }

        .btn--primary {
            background: var(--accent);
            padding: 18px 36px;
            color: white;
        }

        .btn--primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 20px 45px rgba(3, 169, 244, 0.4);
        }

        .btn--secondary {
            background: rgba(255, 255, 255, 0.1);
            border: 2px solid rgba(3, 169, 244, 0.3);
            padding: 16px 34px;
            color: white;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .btn--secondary:hover {
            background: rgba(255, 255, 255, 0.15);
            border-color: var(--accent);
            transform: translateY(-3px);
        }

        .btn--secondary .arrow {
            transition: transform 0.3s ease;
        }

        .btn--secondary:hover .arrow {
            transform: translateX(5px);
        }

        /* ===== SPACER ===== */
        .spacer {
            height: 100vh;
        }

        /* ===== CONTENT SECTION ===== */
        .content {
            position: relative;
            z-index: 10;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 2rem;
            background: #0d0d0d;

            
        }

        .content__title {
            font-size: 3rem;
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .content__text {
            font-size: 1.2rem;
            max-width: 600px;
            line-height: 1.6;
            color: #cccccc;
        }

        /* ===== RESPONSIVE DESIGN ===== */
        @media (max-width: 1400px) {
            .hero__content {
                max-width: 1200px;
                padding: 60px 40px;
                gap: 60px;
            }
            
            .hero__image img {
                width: 700px;
                height: 440px;
            }
            
            .hero__title {
                font-size: 4rem;
            }
        }

        @media (max-width: 1024px) {
            .hero__content {
                flex-direction: column;
                padding: 40px 30px;
                gap: 40px;
                text-align: center;
            }
            
            .hero__image img {
                width: 500px;
                height: 312px;
            }
            
            .hero__title {
                font-size: 3rem;
            }

            .hero__actions {
                justify-content: center;
            }
        }

        @media (max-width: 768px) {
            .nav {
                padding: 20px 25px;
            }
            
            .nav__logo img {
                height: 80px;
            }
            
            .hero__content {
                padding: 30px 20px;
                gap: 30px;
                border-radius: 20px;
                margin: 20px;
                width: calc(100% - 40px);
            }
            
            .hero__image img {
                width: 400px;
                height: 250px;
            }
            
            .hero__title {
                font-size: 2.5rem;
                line-height: 0.9;
            }
            
            .hero__subtitle {
                font-size: 1.1rem;
                margin-bottom: 40px;
            }
            
            .hero__actions {
                flex-direction: column;
                gap: 20px;
                width: 100%;
            }
            
            .btn {
                width: 100%;
                justify-content: center;
            }
        }

        @media (max-width: 480px) {
            .hero__content {
                margin: 10px;
                width: calc(100% - 20px);
                padding: 25px 15px;
                border-radius: 15px;
            }
            
            .hero__image img {
                width: 320px;
                height: 200px;
                border-radius: 15px;
            }
            
            .hero__title {
                font-size: 2rem;
                letter-spacing: -2px;
            }
            
            .hero__subtitle {
                font-size: 1rem;
                margin-bottom: 30px;
            }
            
            .btn {
                padding: 16px 24px;
                font-size: 0.85rem;
            }
            
            .nav__cta {
                padding: 12px 20px;
                font-size: 0.75rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="nav">
        <div class="nav__logo">
            <img src="/src/assets/Logo - Richard Waine Photography (transparent) white.png" alt="Richard Waine Photography">
        </div>
        <button class="nav__cta">SCHEDULE SESSION</button>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero__content">
            <div class="hero__text">
                <h1 class="hero__title">
                    PREMIUM PROFESSIONAL<br>
                    <span class="hero__title--highlight">HEADSHOT PHOTOGRAPHY</span>
                </h1>
                <p class="hero__subtitle">
                    Cinematic portraits that command attention and elevate your professional presence
                </p>
                <div class="hero__actions">
                    <button class="btn btn--primary">VIEW PORTFOLIO</button>
                    <button class="btn btn--secondary">
                        <span>BOOK SESSION</span>
                        <span class="arrow">→</span>
                    </button>
                </div>
            </div>
            <div class="hero__image">
                <img src="/src/assets/images/gallery/600px/(600px) - Ethan Haymovitz_Richard_Waine_Lancaster_PA_0135 - compressed.jpg" alt="Professional Headshot" />
            </div>
        </div>
    </section>

    <!-- Spacer for scroll distance -->
    <div class="spacer"></div>

    <!-- Next Content Section -->
    <section class="content">
        <h2 class="content__title">Next Section</h2>
        <p class="content__text">
            This content appears after the zoom effect completes. Perfect timing and smooth transitions create an immersive user experience.
        </p>
    </section>
</body>
</html>