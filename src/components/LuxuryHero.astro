---
// @ts-nocheck
// LuxuryHero.astro - Premium single-moment hero for luxury headshot photography
---

<div class="luxury-hero">
    <!-- Navigation Header -->
    <nav class="luxury-nav">
        <div class="logo">
            <img src="/assets/Logo - Richard Waine Photography (transparent) white.png" alt="Richard Waine Photography">
        </div>
        <ul class="nav-menu">
            <li><a href="#portfolio">Portfolio</a></li>
            <li><a href="#process">Process</a></li>
            <li><a href="#results">Results</a></li>
            <li><a href="#investment">Investment</a></li>
        </ul>
        <button class="cta-sharp">SCHEDULE CONSULTATION</button>
    </nav>

    <!-- Main Hero Content -->
    <div class="hero-stage">
        <!-- Left Side - Messaging -->
        <div class="hero-content">
            <div class="trust-indicator">
                <span class="rating-stars">★★★★★</span>
                <span class="rating-text">618 Reviews • All 5 Stars</span>
            </div>
            
            <h1 class="hero-title">
                EXECUTIVE<br>
                <span class="title-emphasis">PRESENCE</span><br>
                CAPTURED
            </h1>
            
            <p class="hero-subtitle">
                When your professional image can make or break opportunities, 
                settle only for headshots that command respect and open doors.
            </p>
            
            <div class="hero-actions">
                <button class="btn-primary-sharp">VIEW INVESTMENT</button>
                <button class="btn-secondary-sharp">SEE RESULTS</button>
            </div>
            
            <div class="credibility-strip">
                <div class="credential">
                    <span class="number">618</span>
                    <span class="label">Perfect Reviews</span>
                </div>
                <div class="credential">
                    <span class="number">12+</span>
                    <span class="label">Years Experience</span>
                </div>
                <div class="credential">
                    <span class="number">100%</span>
                    <span class="label">Client Success</span>
                </div>
            </div>
        </div>

        <!-- Right Side - Hero Image -->
        <div class="hero-visual">
            <div class="image-container">
                <div class="image-frame">
                    <img src="/src/assets/images/vendors/Dillon Snyder (16 x 9) - compressed.jpg" alt="Professional Headshot by Richard Waine">
                </div>
                <div class="visual-accent"></div>
            </div>
        </div>
    </div>

    <!-- Bottom Accent -->
    <div class="hero-footer">
        <div class="scroll-indicator">
            <span>Discover the difference</span>
            <div class="scroll-arrow">↓</div>
        </div>
    </div>
</div>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap');
    

    /* Main Container */
    .luxury-hero {
        position: relative;
        width: 100vw;
        height: 100vh;
        background: 
            radial-gradient(ellipse at top left, #0f172a 0%, #000000 50%),
            linear-gradient(135deg, #000000 0%, #0a0a0a 100%);
        font-family: 'Inter', sans-serif;
        color: var(--text-primary);
        overflow: hidden;
    }

    /* Navigation */
    .luxury-nav {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 1000;
        padding: 30px 60px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        backdrop-filter: blur(20px);
        background: rgba(0, 0, 0, 0.4);
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }

    .logo img {
        height: 42px;
        width: auto;
    }

    .nav-menu {
        display: flex;
        gap: 50px;
        list-style: none;
    }

    .nav-menu a {
        color: var(--text-secondary);
        text-decoration: none;
        font-weight: 500;
        font-size: 0.95rem;
        letter-spacing: 0.5px;
        transition: all 0.3s ease;
        text-transform: uppercase;
    }

    .nav-menu a:hover {
        color: var(--accent-blue-light);
    }

    .cta-sharp {
        background: var(--accent-blue);
        border: none;
        padding: 14px 28px;
        color: white;
        font-weight: 700;
        font-size: 0.85rem;
        letter-spacing: 1px;
        text-transform: uppercase;
        cursor: pointer;
        transition: all 0.3s ease;
        clip-path: polygon(0 0, calc(100% - 8px) 0, 100% 100%, 8px 100%);
        box-shadow: 0 8px 25px rgba(37, 99, 235, 0.3);
    }

    .cta-sharp:hover {
        background: var(--accent-blue-light);
        transform: translateY(-2px);
        box-shadow: 0 12px 35px rgba(37, 99, 235, 0.4);
    }

    /* Hero Stage */
    .hero-stage {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 100%;
        max-width: 1400px;
        height: 80%;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 80px;
    }

    /* Hero Content */
    .hero-content {
        flex: 1;
        max-width: 600px;
        z-index: 100;
    }

    .trust-indicator {
        display: flex;
        align-items: center;
        gap: 15px;
        margin-bottom: 30px;
    }

    .rating-stars {
        color: var(--trust-gold);
        font-size: 1.2rem;
    }

    .rating-text {
        color: var(--text-secondary);
        font-weight: 600;
        font-size: 0.9rem;
        letter-spacing: 0.5px;
    }

    .hero-title {
        font-size: 5.5rem;
        font-weight: 900;
        line-height: 0.85;
        margin-bottom: 30px;
        letter-spacing: -4px;
        text-transform: uppercase;
    }

    .title-emphasis {
        background: linear-gradient(135deg, var(--accent-blue) 0%, var(--accent-blue-light) 100%);
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .hero-subtitle {
        font-size: 1.4rem;
        color: var(--text-secondary);
        line-height: 1.6;
        margin-bottom: 50px;
        font-weight: 400;
        max-width: 520px;
    }

    .hero-actions {
        display: flex;
        gap: 25px;
        margin-bottom: 60px;
    }

    .btn-primary-sharp {
        background: var(--accent-blue);
        border: none;
        padding: 18px 36px;
        color: white;
        font-weight: 700;
        font-size: 0.95rem;
        cursor: pointer;
        transition: all 0.3s ease;
        letter-spacing: 1px;
        text-transform: uppercase;
        clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 100%, 12px 100%);
        box-shadow: 0 12px 30px rgba(37, 99, 235, 0.3);
    }

    .btn-primary-sharp:hover {
        background: var(--accent-blue-light);
        transform: translateY(-3px);
        box-shadow: 0 18px 40px rgba(37, 99, 235, 0.4);
    }

    .btn-secondary-sharp {
        background: var(--glass);
        border: 2px solid var(--glass-border);
        padding: 16px 34px;
        color: var(--text-primary);
        font-weight: 700;
        font-size: 0.95rem;
        cursor: pointer;
        transition: all 0.3s ease;
        letter-spacing: 1px;
        text-transform: uppercase;
        backdrop-filter: blur(10px);
        clip-path: polygon(0 0, calc(100% - 12px) 0, 100% 100%, 12px 100%);
    }

    .btn-secondary-sharp:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: var(--accent-blue);
        transform: translateY(-3px);
    }

    .credibility-strip {
        display: flex;
        gap: 40px;
    }

    .credential {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
    }

    .credential .number {
        font-size: 2.2rem;
        font-weight: 800;
        color: var(--accent-blue);
        line-height: 1;
    }

    .credential .label {
        font-size: 0.85rem;
        color: var(--text-secondary);
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-top: 5px;
    }

    /* Hero Visual */
    .hero-visual {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: center;
        position: relative;
    }

    .image-container {
        position: relative;
        width: 600px;
        height: 337px;
        /* 16:9 aspect ratio */
    }

    .image-frame {
        position: relative;
        width: 100%;
        height: 100%;
        overflow: hidden;
        clip-path: polygon(0 0, calc(100% - 30px) 0, 100% 100%, 30px 100%);
        box-shadow: 
            25px 25px 60px rgba(0, 0, 0, 0.8),
            -25px -25px 60px rgba(255, 255, 255, 0.02);
    }

    .image-frame img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: top center;
        filter: contrast(1.1) saturate(1.1);
    }

    .visual-accent {
        position: absolute;
        top: -20px;
        right: -20px;
        width: 150px;
        height: 150px;
        background: linear-gradient(135deg, var(--accent-blue), var(--accent-blue-light));
        clip-path: polygon(0 0, calc(100% - 20px) 0, 100% 100%, 20px 100%);
        opacity: 0.1;
        z-index: -1;
    }

    /* Hero Footer */
    .hero-footer {
        position: absolute;
        bottom: 40px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 100;
    }

    .scroll-indicator {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 10px;
        color: var(--text-secondary);
        font-size: 0.9rem;
        font-weight: 500;
        letter-spacing: 0.5px;
        text-transform: uppercase;
    }

    .scroll-arrow {
        font-size: 1.5rem;
        animation: bounce 2s infinite;
    }

    @keyframes bounce {
        0%, 20%, 50%, 80%, 100% {
            transform: translateY(0);
        }
        40% {
            transform: translateY(-10px);
        }
        60% {
            transform: translateY(-5px);
        }
    }

    /* Responsive */
    @media (max-width: 1200px) {
        .hero-stage {
            flex-direction: column;
            text-align: center;
            gap: 40px;
        }
        
        .hero-content {
            max-width: 100%;
        }
        
        .hero-title {
            font-size: 4rem;
        }
        
        .image-container {
            width: 480px;
            height: 270px;
        }
    }

    @media (max-width: 768px) {
        .luxury-nav {
            padding: 20px 30px;
        }
        
        .nav-menu {
            display: none;
        }
        
        .hero-title {
            font-size: 3rem;
        }
        
        .hero-actions {
            flex-direction: column;
            gap: 15px;
        }
        
        .credibility-strip {
            justify-content: center;
            gap: 30px;
        }
        
        .image-container {
            width: 320px;
            height: 180px;
        }
    }
</style>